{
  "name": "hwpx-viewer",
  "version": "0.8.0",
  "private": true,
  "description": "HWPX Viewer — 한국어 공공/기업 문서를 웹에서 보고 AI로 편집하는 뷰어/에디터",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev": "pnpm -C apps/web dev",
    "build": "pnpm -C apps/web build",
    "test": "pnpm -C apps/web test",
    "lint": "pnpm -C apps/web lint",
    "typecheck": "pnpm -C apps/web typecheck"
  },
  "devDependencies": {
    "typescript": "5.5.4"
  }
}
